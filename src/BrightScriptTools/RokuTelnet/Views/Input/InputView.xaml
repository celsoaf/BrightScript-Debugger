<UserControl x:Class="RokuTelnet.Views.Input.InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBox Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}" Background="Black" Foreground="White" FontFamily="Consolas" Name="InputBlock" BorderBrush="{x:Null}" SelectionBrush="{x:Null}"
                 IsEnabled="{Binding Enable}">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding EnterCommand}"/>
                <KeyBinding Key="Up" Command="{Binding UpCommand}"/>
                <KeyBinding Key="Down" Command="{Binding DownCommand}"/>
            </TextBox.InputBindings>
        </TextBox>
    </Grid>
</UserControl>
